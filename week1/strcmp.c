#include <stdio.h>
int compare(char[],char[]);
int main()
{
    int i,j,limit;
    char a[20];
    char b[20];
    printf("enter the first string\n");
    scanf("%s",a);
    printf("enter the seciond string\n");
        scanf("%s",b);
limit=compare(a,b);
    if(limit])
    {
    printf("\nthe str is not same first is high");
    }
    else if(a[i]!=b[i]) 
    {
    printf("\nthe str is not same second is high");
    }else
    {
        printf("its same\n");
    }
    return 0;
}